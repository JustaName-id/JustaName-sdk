import { IconGallery, IconItem, Meta } from '@storybook/blocks';
import React from 'react';
import { contentHash } from '../../lib/icons';

<Meta title="Iconography/ContentHash" />

# ContentHash


<IconGallery>

  {
    Object.keys(contentHash).map((icons, index) => {
      const iconElement = contentHash[icons];
      return(
      <IconItem key={index} name={icons} title={icons}>
        {React.createElement(iconElement, { size: 32 })}
      </IconItem>
    )})
  }

</IconGallery>
