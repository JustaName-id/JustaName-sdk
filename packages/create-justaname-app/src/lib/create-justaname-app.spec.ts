import { createJustanameApp } from './create-justaname-app';

describe('createJustanameApp', () => {
  it('should work', () => {
    expect(createJustanameApp()).toEqual('create-justaname-app');
  });
});
